{
  "name": "stratton-security",
  "version": "0.1.0",
  "private": true,
  "description": "Stratton Security Group marketing site (static, built from a baosh-based template).",
  "scripts": {
    "build": "node build.js",
    "start": "node build.js && npx --yes serve public",
    "preview": "npx --yes serve public"
  }
}
